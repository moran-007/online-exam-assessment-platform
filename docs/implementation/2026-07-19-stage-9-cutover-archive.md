# Stage 9 全量迁移、切换与归档实施记录

日期：2026-07-19  
状态：本地切换、观察验证与旧服务关闭完成；外部 Secret 撤销待供应商控制台确认

## 实施结果

- T-7 首轮对账捕获旧库新增的 4 个课次和 2 个家长身份差异，没有放行切换。
- 身份预检运行 `445f8ee8-3e6c-4d0d-9638-689228967957` 已完成冲突处置、批准和幂等导入；两个家长姓名歧义按测试数据策略保留签字记录，不按姓名合并。
- 4 个新增课次已迁移，最终源数据为学生 46、教师 16、家长候选 32、班级 17、班级成员 51、课次 32、考勤 55、课时期初 46、教学记录 12、Scratch 作品 14。
- 22 类实体中 21 类严格一致；家长为 30 条映射加 2 条已批准冲突，目标缺失记录为 0。
- 49 个迁移附件映射全部存在，目标文件缺失、大小差异和 SHA-256 差异均为 0；6 类关键外键孤儿均为 0。
- worker 请求层只读、SQLite `mode=ro`、跳过启动初始化三重验证通过；真实启动后写请求返回 503，源库 SHA-256 保持不变。
- 主平台 3000/5173 入口已启动，旧服务 8000 无监听；切换状态进入 `ACTIVE_MAIN`，激活与观察报告均通过。
- 固定全量质量门通过：43 个单元套件/147 项、6 个集成套件/22 项和完整 Chromium 点击回归全部成功；归档前再次对账通过。
- worker 状态已进入 `ARCHIVED`；旧 API 返回 410、旧页面跳转主平台，启动后源指纹保持不变。

## 备份与恢复

- 备份目录：`output/backups/backup-2026-07-19-134723`。
- 恢复演练在隔离数据库执行后自动清理。
- 33 个数据库迁移、28 类关键表计数、178 个上传文件全部一致。
- 当前未配置 `BACKUP_REMOTE`，备份仍只在本机；生产切换前必须配置异机或对象存储副本。

## 工程交付

- `scripts/worker-cutover.ts`：rehearse、freeze、activate、verify、rollback-entry、close 状态机。
- `scripts/cutover/worker-source.ts`：只读源快照、四条迁移指纹、实体与附件清单。
- `scripts/cutover/worker-reconciliation.ts`：目标存在性、签字差异、哈希和完整性检查。
- `scripts/backup.ts`：备份清单扩展到教务、Scratch、迁移和审计关键表。
- `scripts/start-platform.ps1`：切换状态下停止旧服务端口。
- worker_01：统一维护闸门、数据库只读连接、公开维护状态和自动化测试。
- class_worker：标记 superseded，旧一键启动入口拒绝启动。

## 回滚边界

允许回滚产品入口，不允许删除 worker 维护标记、恢复旧写入或建立反向双写。源数据指纹、迁移运行、对账报告和审计日志共同构成切换证据链。

## 尚需外部确认

用户曾在对话中暴露阿里云与混元 Key。三个 Git 仓库均未发现这些 Key 的明文，主平台配置为加密存储；对应供应商配置已停用并记录 `rotation_required` 审计状态。供应商侧撤销仍必须由有控制台权限的人员完成。撤销并换入新 Key 后，Stage 9 才满足“所有旧 Secret 已撤销”的最终退出条件。
