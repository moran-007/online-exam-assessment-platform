# 阶段 2：考试总结 MVP 实施记录

> 日期：2026-07-16  
> 状态：确定性预览与任务执行链完成；人工审核/发布和页面闭环进行中

## 已完成

- `ExamSummaryDatasetBuilder` 复用统计查询，先做考试范围鉴权，再生成不含个人身份和答题正文的事实快照。
- 参与、成绩、分布、题目和知识点数值均绑定 `evidenceRef`；输入哈希排除采集时间，保证同一事实集稳定命中。
- `GET /api/v1/ai-summaries/exams/:examId/preview` 在模型不可用时仍可展示确定性统计。
- `POST /api/v1/ai-summaries/exams` 支持自动选择默认配置，也支持手动选择当前用户可使用的模型配置。
- 考试总结 Prompt v1 和 JSON Schema 作为受审版本写入数据库；只有记录审核人后模板才能启用。
- 任务持久化输入快照、请求 Token 上限、模型/模板/Schema 版本和 correlationId。
- 唯一幂等键覆盖事实哈希、数据集、Prompt、Schema、Provider 和模型；成功结果复用不再次调用或计费。
- 模型调用不放在数据库事务内；结构化输出和证据引用全部通过后，才在短事务中原子写入总结草稿与成功状态。
- 超时或连接中断若可能已产生费用，写入“用量未报告”流水；配置本地预算时会暂停后续调用，避免余额失真。
- 任何生成结果都只创建 `draft`，任务执行链没有自动发布路径。

## 数据库迁移

`20260716143000_add_exam_summary_prompt_v1` 新增任务输入快照、请求输出 Token 上限和考试总结 Prompt v1。迁移在本地开发库与 `online_exam_test` 顺序部署成功；若没有超级管理员，模板会安全地保持禁用。

## 当前验证

- 单元测试：81/81 通过，覆盖成功生成、幂等复用、失败重试、Schema 失败和超时未报告用量。
- 集成测试：9/9 通过；替身模型第一次生成草稿，第二次相同输入零调用复用，Token 流水只有一条。
- ESLint、Nest build、Prisma format/validate/generate 和前端 typecheck 通过。
- 本阶段尚未发送真实模型请求，真实 Token 消耗仍为 0；供应商余额未知。

## 待完成

- 任务状态/考试历史查询接口。
- 教师编辑、送审、发布、撤回和审计事件。
- 学生仅查看本人已发布总结。
- 考试结果页入口、50 个脱敏黄金样本和浏览器测试。
- 闭环完成后，由教师在个人配置页面录入测试密钥，执行约 1,000 输出 Token 的最小真实验收。
